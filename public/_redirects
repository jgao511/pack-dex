# Canonical publisher and noindexed utility routes are exact build artifacts.
# Mobile deep links are handled by the scoped Pages Function. Do not add a root,
# set-route, utility, or mobile catch-all that could swallow static assets.
